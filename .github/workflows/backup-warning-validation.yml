name: Temporary backup warning validation
on:
  push:
    branches:
      - fix/printer-detail-maintenance-shortcut-v0240
jobs:
  test:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - run: npm install
      - run: npm test
      - run: npm run build:bundle
